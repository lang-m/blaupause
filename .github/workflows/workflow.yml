name: workflow
on: [push]

jobs:
  workflow:
    runs-on: ubuntu-latest
    env:
      TEST: $$GITHUB_WORKSPACE/something
    steps:
      - name: home
        run: |
          echo $HOME
          ls -la
      - name: checkout
        uses: actions/checkout@v2

      - name: test-env
        run: |
          echo pwd
          echo $GITHUB_WORKSPACE
          echo $TEST
      - name: read
        run: ls -la
